Remove the `rounded bg-white p-1` classes from the footer logo `<img>` in `src/components/site/SiteFooter.tsx` so the transparent PNG shows through against the grey footer. Header keeps its white background (logo sits in the teal-adjacent header area where transparency would clash).