# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
.output
.vinxi
.tanstack/**
.nitro
*.local

# Wrangler / Cloudflare
.wrangler/
.dev.vars

# Claude Code agent worktrees (local tooling scratch space)
.claude/worktrees/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Output of scripts/e2e-gallery.ts (published to Pages / uploaded as a CI
# artifact, never committed)
gallery/
gallery-*/

# Ids of the people scripts/seed-local-club.mjs put in the local database.
# Regenerated on every seed, and meaningless against any other database.
.local-club-fixture.json

# Playwright end-to-end run output: the HTML report, per-test traces/videos,
# and the persona sessions e2e/support/auth.setup.ts saves (real access tokens
# for the local stack, regenerated on every run).
playwright-report/
test-results/
blob-report/
e2e/.auth/
